import ServicePage from '../ServicePage'
import content from '../../../../content/content.json'

export default function TotalReconstructionComponent() {
  return <ServicePage data={content.total_reconstruction} />
}
